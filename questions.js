/* ============================================================
 *  questions.js — מאגר השאלות. זה הקובץ שגדל הכי הרבה.
 *  נוצר בשלב 2 של המדריך. שתי הדוגמאות למטה מראות את הסכימה.
 * ============================================================ */

/* --- שאלה אמריקאית (4 אפשרויות) --- */
window.CODEX_ADAPTIVE_QUESTIONS = [
  {
    id: "Q01",
    cat: "<קטגוריה>",            /* חייב להתאים ל-config.js */
    difficulty: 3,                /* 1–4 */
    skills: ["<מיומנות>"],
    origin: "recent",             /* recent | official | homework | generated */
    sourceLabel: "מבוסס <שנה> · שאלה <מספר>",

    prompt: "<b>נוסח השאלה המלא</b> — כל מה שצריך כדי לפתור.",
    code: "for (i = 0; i < n; i++)\n    cnt++;",   /* אופציונלי */
    kind: "code",                 /* code | math — משפיע על העימוד */

    opts: [                       /* בדיוק 4 */
      { h: "O(1)",  ltr: true },
      { h: "O(n)",  ltr: true },
      { h: "O(n²)", ltr: true },
      { h: "O(2ⁿ)", ltr: true }
    ],
    ans: 1,                       /* אינדקס התשובה הנכונה, מ-0 */

    rule: "הכלל בשורה אחת.",
    why:  "ההסבר המלא — למה זו התשובה.",
    trap: "המלכודת: מה מוביל לתשובה השגויה."
  }
];

/* --- שאלה פתוחה (סימון עצמי) --- */
window.BANK_OPEN_QUESTIONS = [
  {
    id: "BK-1.1",
    cat: "<קטגוריה>",
    difficulty: 3,
    skills: ["<מיומנות>"],
    origin: "bank",
    kind: "open",                 /* חובה — מפעיל את מסלול השאלה הפתוחה */
    sourceLabel: "🎓 מבחן <שנה> · שאלה <מספר>",

    prompt: "<b>נוסח מלא ועומד בפני עצמו:</b> מה נתון, מה לחשב, ואילו אילוצים.",
    hint: "רמז אחד שמכוון בלי לפתור.",

    qtype: "code",                /* code | trace | analysis | design */
    signature: "int foo(int n)",  /* רק ל-qtype:"code" — פותח עורך קוד */

    /* גוף השאלה: הקוד/הנתונים + הפתרון בתוך <details class="sol">.
       המנוע מפצל אוטומטית — הקוד מוצג, הפתרון רק אחרי "בדוק". */
    html: '<pre class="c">int foo(int n){ /* … */ }</pre>' +
          '<details class="sol"><summary>פתרון</summary><div class="body">' +
          '<p>ההסבר המלא.</p></div></details>'
  }
];

/* ============================================================
 *  אם יש גם מאגר גדול בפורמט אחר, אפשר להזין אותו כך:
 *  var CODEX_COMPLEXITY_DATA = { qs: [ … ], cats: [ … ] };
 *  המנוע ממזג את שלושת המקורות לרשימה אחת.
 * ============================================================ */
var CODEX_COMPLEXITY_DATA = { qs: [], cats: [] };
